# 前端大改方案（草案 v1，待确认）

> 本方案仅规划，未落任何代码。确认后再动手。
> 范围：前端大改。后端仅在"评估进行中每节点结论"的数据来源上可能需要小改（见 §6 / §8-2）。

---

## 0. 重要事实校正（影响"路线 B"含义）

经核对 `package.json`：当前前端**实际只依赖** `react / echarts / react-router-dom + tailwind`，
**并没有安装 shadcn/ui**。现有组件是手写 Tailwind（`src/components/` 下只有
`AdvisorPanel / CourtRoom / RerunControl`，无 `ui/` 组件库）。之前方案文档写的 shadcn 未真正落地。

因此此前你选的"路线 B（保留 shadcn + 借 TDesign token）"真实落地有两种子选项：

- **B1｜纯手写 Tailwind + 注入 TDesign token**：最轻、最自由，组件继续手写。
- **B2｜引入 shadcn/ui + 注入 TDesign token**：规范组件现成、可访问性更好，且 shadcn 是 copy-in（代码在你仓库），自由度仍够。更贴合"评估/案件要规范"的诉求。

**建议 B2**：规范向组件直接可用，又保留你重视的改动自由度。

---

## 1. 设计基线（已与你确认）

- **分场景双模式**：评估/案件 = 规范（TDesign 清晰/一致）；模拟法庭 = 剧场动画（motion + 空间布局 + 可控 shader 背景）。
- **统一 token 系统**：两种模式共享同一套色彩/间距/字体，靠"明场规范 / 暗场剧场"上下文切换区分，不是两个产品。
- **技术栈不变**：Vite / React / TS / ECharts / SSE 全保留。仅追加 `framer-motion`（动画）；
  shader 背景可选 `@react-three/fiber`（仅模拟法庭）。

---

## 2. 评估流程清晰度专项（你的硬性要求：每个环节信息/结论都要清晰）

### 2.1 现状问题（已读代码确认）
- `Evaluation.tsx` 运行/完成视图**只显示状态圆点 + 等宽日志**，**不含任何节点的分数、结论、依据**。
- `NODE_ORDER` 漏了 `damages / recovery / precedent`（要钱/要名分支），流程文案也不准。
- SSE 事件 `EvalEvent` 只带 `status`，**不带 score/analysis** → 前端拿不到每环节结论。
- `DecisionDashboard.tsx` 已较清晰（分数/子维度/rerun/stale/缺口），但它是"结果页"，评估进行中看不到。
- 结论：评估"进行中"视图是清晰度主战场；需与结果页衔接。

### 2.2 目标形态：评估流程 = 可追溯的步骤时间线
按真实后端顺序分轴（前端当前缺失的分支补回）：

- **前置阶段**：证据盘点 → 硬门禁检查
- **法律可行性轴**：权利基础 → 侵权认定 → 诉讼程序
- **业务预期轴**：要钱 = 判赔规模 → 回款能力；要名 = 判例价值
- **决策合成（终）**

每个节点卡片展示：**状态徽标 + 分数（大字，带档位色） + 一句结论 + 关键依据（要素拆解 / 红线绿灯） + 重跑入口 + 失效标记**。

数据来源：节点进行中用 SSE 状态；节点完成后立即取该节点 `dimension_results` 的 `score + result`
（analysis / 要素 / red/green flags）渲染到卡片，做到"实时清晰"。

### 2.3 清晰度原则（落到每个环节）
- 分数必带刻度与档位色（≥go 绿 / patch–go 黄 / <patch 红），不裸数字。
- **结论先于细节**：每节点先一句话结论，再展开依据。
- 红线/绿灯用统一语义色（现状 `stateIcon` 硬编码 `blue/green/red/amber` 与主题 `ink/ember` 不一致 → 改 token 语义色）。
- 失效（stale）显式标"已过期，建议重跑"，不静默。
- 失败节点说明影响范围（如"回款能力失败 → 业务预期未完成"），不假装 0。

---

## 3. 信息架构 / 页面重排

- 路由保持不变。重点重做：`Evaluation`（步骤时间线）、`MootCourt`（剧场模式）、
  `DecisionDashboard`（已较好，仅统一 token）、`Report` / `OnePager`（规范统一）。
- 新建轻量组件层（基于 Tailwind + TDesign token）：`Card / Badge / StatusDot / ScoreBar /
  StepperNode / ConclusionCard / Tabs / Dialog / Tooltip / EmptyState`。
- 模拟法庭剧场模式：`CourtRoom` 重做——原告居左、被告居右、法官居中；回合滑动入场；
  修正系数回写脉冲；可选 shader 背景。

---

## 4. Token / 主题方案（已确认：评估/案件走 A 白底极简，模拟法庭走暗色剧场例外）

### 4.1 两套主题，共享结构 token（同一套组件，仅颜色层不同）
- **评估/案件主题（规范·A，取自截图画廊页 · 白底极简）**
  - `--bg:#ffffff`、`--surface:#f7f8fa`、`--text:#0d0d0d`、`--text-muted:#6b7280`、`--border:#e5e7eb`
  - 强调色 `--brand` 取近黑 `#0d0d0d`（贴合画廊黑按钮/黑字），极简单色
  - 语义色（TDesign 亮底标准）：`--success/--warning/--danger/--info` 给红线/绿灯/档位
- **模拟法庭主题（剧场·暗色例外，取自实际 shader 落地页）**
  - `--bg:#0b0b0b`、`--surface:#14141a`、`--text:#ffffff`、`--text-muted:rgba(255,255,255,.62)`、`--border:rgba(255,255,255,.10)`
  - 强调色 `--brand` 电光紫 `#be7cff`、`--brand-2` 青 `#5dd6ff`、`--brand-deep` 深度蓝 `#1c6f91`
  - 释放紫/青辉光（仅剧场页，文字高对比压其上）
- 两套共享 spacing/radius(4/8/12)/fontSize 阶梯/shadow/字体 → 仍是"一个产品"

### 4.2 TDesign token 接入（B2）
- `tailwind.config.js` 扩展 TDesign 设计 token（spacing/radius/字号/shadow/语义色）；颜色层用双主题 CSS 变量
- `index.css`：`:root` 默认明场 + `[data-theme="theater"]`（或 `.theme-theater` class）切暗场；
  全局删硬编码 `blue-500`/`ink`/`ember`，统一走 token

### 4.3 双模式靠"明场规范 / 暗场剧场"区分
- 评估/案件：明场、白底黑字、大量留白与结构、无辉光（贴合 TDesign 清晰/一致）
- 模拟法庭：暗场、紫青辉光 + framer-motion 编排（剧场例外，强对比凸显沉浸感）

---

## 5. 动画方案（仅模拟法庭 + 转场）

- `framer-motion`：节点入场/退场、回合滑动、系数回写脉冲、loading 骨架。
- 评估/案件页：克制微交互（hover、展开），不抢内容。
- shader 背景：`@react-three/fiber` 画布，仅挂 `MootCourt` 房间背后，文字高对比压其上；
  提供"关闭动效"开关（无障碍 / 性能）。

---

## 6. 涉及文件清单（草案）

**改**
- `frontend/tailwind.config.js`、`frontend/src/index.css`（token 基座）
- `frontend/src/pages/Evaluation.tsx`（大改：步骤时间线）
- `frontend/src/pages/DecisionDashboard.tsx`（微调：统一 token）
- `frontend/src/pages/MootCourt.tsx` + `src/components/CourtRoom.tsx`（剧场模式）
- `frontend/src/pages/Report.tsx` / `OnePager.tsx`（规范统一）
- `frontend/src/api.ts`（EvalEvent 补 score/analysis，或加 per-node result 拉取）

**可能改后端**
- `backend/core/orchestrator.py`：SSE 事件补发 `score/analysis`（或新增
  `GET /evaluation/{id}/node/{node}` 端点，供前端逐节点取结论）

**新建**
- `frontend/src/components/ui/*`（Card/Badge/ScoreBar/StepperNode/ConclusionCard 等）
- `frontend/src/theme/tokens.ts`（TDesign token 映射）
- `frontend/src/hooks/useEvalFlow.ts`（流程状态机）

**可能加依赖**
- `framer-motion`（必加）；`@react-three/fiber` + `three`（模拟法庭 shader，可选）

---

## 7. 实施分期（建议，每期独立确认）

- **P1 评估流程清晰度**（你最关心的）：重做 `Evaluation` 步骤时间线 + 后端补发结论 + 统一语义色。
- **P2 Token/主题基座**：TDesign token 注入 + 组件层 + 全局去硬编码色。
- **P3 模拟法庭剧场模式**：framer-motion + 可选 shader 背景。
- **P4 报告 / 一页纸规范统一 + 其余页微调。

---

## 8. 决策状态（全部确认）

**已确认**
1. 路线 **B2**：引入 shadcn/ui + TDesign token
2. 评估进行中每节点结论：**前端逐节点拉 `api.result`**（不改后端）
3. 配色（分场景）：评估/案件 = **A 白底极简**（白底/黑字/灰阶中性，取自截图画廊页，见 §4.1）；
   模拟法庭 = **暗色剧场例外**（近黑底 + 电光紫 `#be7cff` / 青 `#5dd6ff` 辉光，取自实际 shader 落地页）
4. 模拟法庭：**先纯 motion，不加 3D**（日后可加 r3f 增强）
5. 分期：**P1→P4，每期独立确认**

（无待确认项，方向已锁定，可进入 P1 实施）
