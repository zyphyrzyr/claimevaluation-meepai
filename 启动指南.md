# Soft IP v4 启动指南

前后端分离项目（FastAPI + React/TS + Vite）。本文件记录本地启动方式，便于随时查看。

## 一、一条命令拉起前后端（推荐）

项目根目录的 `start.sh` 会同时启动后端（:8000）和前端（:5173），
在终端里 `Ctrl+C` 会通过 `trap` 同时停掉两个进程。

```bash
# 真实模式：跑真实 Deepseek / 企查查 / 北大法宝（即已验证过的真实链路）
cd /Users/zhaoyirui/WorkBuddy/2026-08-25-17-14-24/soft-ip-v4 && USE_MOCK=False bash start.sh

# mock 模式：秒开、不烧 token，适合纯前端改动阶段
cd /Users/zhaoyirui/WorkBuddy/2026-08-25-17-14-24/soft-ip-v4 && USE_MOCK=True bash start.sh
```

## 二、备用：分开手动启动（start.sh 出问题时用）

```bash
# 后端
cd /Users/zhaoyirui/WorkBuddy/2026-08-25-17-14-24/soft-ip-v4/backend && \
  /Users/zhaoyirui/.workbuddy/binaries/python/envs/default/bin/python -m uvicorn main:app --reload --port 8000

# 前端（另开一个终端）
cd /Users/zhaoyirui/WorkBuddy/2026-08-25-17-14-24/soft-ip-v4/frontend && \
  /Users/zhaoyirui/.workbuddy/binaries/node/versions/22.22.2-3/bin/npm run dev
```

## 三、访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端 | http://localhost:5173/ | 实际使用入口 |
| 后端 API | http://localhost:8000/ | FastAPI 接口 |
| API 文档 | http://localhost:8000/docs | Swagger UI |

## 四、必须知道的坑

1. **`start.sh` 默认是 mock 模式**。脚本里写死 `USE_MOCK=${USE_MOCK:-True}`，
   会覆盖 `.env` 中的 `USE_MOCK=False`。要跑真实链路**必须显式加 `USE_MOCK=False`**。
2. **真实环境变量优先于 `.env`**（`config.py` 行为）。所以启动时命令行带的
   `USE_MOCK=...` 一定生效，不用再去改 `.env`。
3. **node 路径已修**：`start.sh` 中的 `NODE` 为
   `/Users/zhaoyirui/.workbuddy/binaries/node/versions/22.22.2-3/bin`
   （原 `22.22.2` 缺 `-2` 后缀会导致 `npm` 找不到、前端起不来）。若换环境请核对该路径。
4. **真实模式评估耗时**：每次点「评估」会真实调用 Deepseek（约 30s–2min）+
   企查查/北大法宝，前端改动阶段建议切 mock 模式提速。

## 五、端口占用

- 后端固定 :8000，前端固定 :5173。
- 启动前若提示端口被占，先查杀残留进程：
  ```bash
  lsof -ti :8000 | xargs kill -9   # 释放后端端口
  lsof -ti :5173 | xargs kill -9   # 释放前端端口
  ```

## 六、外部密钥（真实模式必需）

真实模式依赖 `.env` 中的以下密钥，缺失会走诚实降级（不会伪造数据）：
- `LLM_API_KEY`（DeepSeek）
- `QCC_API_TOKEN`（企查查回款能力）
- `PKULAW_API_TOKEN`（北大法宝法条检索，仅报告增强、不参与评分）
- `SILICONFLOW_API_KEY`（硅基流动 bge-m3 embedding，RAG 语义向量；为空时 RAG 走 mock 哈希向量，不影响主链路）
